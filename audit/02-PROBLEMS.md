# 02 — Problems

Format: `ID | Severity | Area | Finding | file:line | Business impact | Fix effort | Fix summary`
Severity: P0 = revenue-blocking today, P1 = growth-blocking, P2 = debt, P3 = polish.
Sorted by severity, then business impact. Every row was verified by direct code read on 2026-08-06 (not inferred from the older 2026-08-04 internal audit — where that audit's claim no longer reproduces, it is noted in `01-INVENTORY.md §11` instead of repeated here).

## P0 — revenue-blocking today

| ID | Severity | Area | Finding | file:line | Business impact | Fix effort | Fix summary |
|---|---|---|---|---|---|---|---|
| P0-1 | P0 | Data integrity / SEO | Root layout emits a sitewide FAQPage JSON-LD block, present on **every page**, with distance/duration facts that contradict the actual route detail pages: Jeddah Airport→Makkah stated as "85 km... 1 hour 15 minutes" vs the route page's own data of 80 km / 60 min; Makkah→Madinah stated as "450 km... 4 hours 30 minutes" vs 430 km / 240 min on the actual route/location pages. | `app/layout.tsx:151-155`, `:165-171` vs `lib/data/routes.ts:8-9` (jeddah-airport-to-makkah) and `:527` (makkah-to-madinah) | Google and AI answer engines read conflicting facts about the same entity from the single most-crawled block on the site (it's in `<head>` of every page) — this is the exact mechanism 00-FINDINGS.md flagged as poisoning structured data and eroding trust/rankings sitewide, not just on one page. | S | Delete the hardcoded numbers from the layout FAQ; generate the answer text from `ROUTES_DATA` (or reference the specific route's `tldrFacts`) so the sitewide FAQ can never drift from the page-level facts. |
| P0-2 | P0 | Pricing / conversion | The live price calculator's distance/duration resolution falls back to a **non-geographic formula** when no Google Maps key is present: `distance = max(30, min(650, (pickup.length+dropoff.length) * 4.5))`, `duration = distance * 0.9`. `NEXT_PUBLIC_GOOGLE_MAPS_KEY` is confirmed **absent** from `.env.local` (not present as a key at all). This same distance/duration feeds the WhatsApp prefill message and `/api/bookings`. | `app/api/pricing/route.ts:38,61-65` | If production also lacks this key (UNVERIFIED — see verification command below), every "Instant Price Calculator" quote on the site is fabricated from address text length, not real geography — a 20 km airport hop can render as 90 km / 81 min. This is the exact "1.5 hours for 20 km" symptom in 00-FINDINGS and is the single most direct explanation for lost bookings: a visitor compares the quoted price/time to Uber/Careem and leaves. | S (add the key) / M (add a fallback that degrades gracefully instead of fabricating numbers) | Add `NEXT_PUBLIC_GOOGLE_MAPS_KEY` to the Vercel production environment (verify with `vercel env ls`); until then, replace the char-count fallback with a static distance table keyed by known city/airport pairs (already exists in `ROUTES_DATA`) so unrecognized free-text input degrades to "request a quote on WhatsApp" instead of a fake number. |
| P0-3 | P0 | Data integrity | Homepage renders two of its own hardcoded route-fact arrays that contradict `ROUTES_DATA`, the source that actually powers the route detail pages. Most severe: the homepage's "Popular Umrah Routes" card shows Madinah→Jeddah Airport at **SAR 549**, while the route page for that exact route (driven by `ROUTES_DATA`) shows **SAR 380** — a 44% price gap on the same page-set, exactly the contradiction flagged in 00-FINDINGS.md. | `components/sections/home-page.tsx:1409` (549) vs `lib/data/routes.ts:397` (380, `madinah-to-jeddah-airport` slug) | This happens on the highest-traffic page (homepage) in the highest-value segment (Umrah pilgrims returning to the airport) — a visitor who clicks through from the homepage price to the route page sees the price drop by SAR 169, which reads as bait-and-switch and kills trust at the exact moment of booking intent. | S | Delete the two hardcoded arrays (`home-page.tsx:530-536` and `:1406-1410`); render both homepage widgets from `ROUTES_DATA` by slug lookup instead of retyped literals. |
| P0-4 | P0 | Data integrity | Even inside the single "source of truth" file, reverse-direction routes are hand-entered as separate records and have drifted: Jeddah→Madinah is SAR 549 while Madinah→Jeddah is SAR 350 for the same 420 km corridor (36% cheaper reverse, unexplained); Madinah→Jeddah Airport is SAR 380 while Jeddah Airport→Madinah is SAR 549. | `lib/data/routes.ts:36` (jeddah-to-madinah, 549) vs `:410` (madinah-to-jeddah, 350); `:49` (jeddah-airport-to-madinah, 549) vs `:397` (madinah-to-jeddah-airport, 380) | A customer who prices the return leg of their own trip sees an unexplained ~36–44% swing — reads as either a pricing error or an attempt to overcharge one direction, either of which loses the booking. | S | Store one canonical `{distanceKm, durationMin, basePrice}` per undirected corridor and derive both directional slugs/pages from it, unless there's a real operational reason (e.g., empty-return deadheading) for asymmetric pricing — if so, state that reason on the page instead of silently showing two numbers. |
| P0-5 | P0 | Trust / legal | Marketing copy asserts "ZATCA-Compliant E-Invoice," "ZATCA-certified," and "VAT-registered" in at least 5 places, while `lib/config/credentials.ts` — the file explicitly built to gate these exact claims — has `vatNumber: null`, `commercialRegistration: null`, `motLicenseNumber: null`, `legalEntityName: null`, all marked `// TODO`. The gating pattern (`hasCredential()`) is applied in exactly one place (the price calculator's MoT license line) and nowhere else. | Claims: `components/sections/home-page.tsx:116`, `app/about/page.tsx:62`, `app/(marketing)/services/corporate/page.tsx:35,49,89,152,187`, `app/(marketing)/services/business-executive/page.tsx:12,121`, `components/layout/Footer.tsx:16,298`. Gate definition (violated everywhere but one place): `lib/config/credentials.ts:8-17` | These claims are concentrated on exactly the pages targeting the owner's stated growth segment (corporate accounts, business-executive) — a corporate finance department that requests the real VAT number for an expense claim and gets none will not open a monthly account, and an unbacked regulatory claim is a genuine legal exposure in Saudi consumer-protection terms, not just a UX nit. | S | Either obtain and enter the real numbers in `credentials.ts` (best outcome), or wrap every one of the 5+ locations above in `hasCredential(...)` the same way `PriceCalculator.tsx:605-608` already does, so the claim disappears until it's true. |
| P0-6 | P0 | Trust / legal | The ZATCA invoice generator hardcodes a legal entity name that does not match the site's brand, and falls back to a placeholder VAT number when unconfigured: `sellerName: "Riyadh Luxe Transportation Co."`, `sellerVATNumber: process.env.ZATCA_VAT_NUMBER ?? "300000000000003"`. `ZATCA_VAT_NUMBER` is confirmed absent from `.env.local`. Submission itself is simulated when `ZATCA_API_URL`/`ZATCA_CERTIFICATE` are unset (also absent): `console.log("[ZATCA SIMULATION]...")`, returns a fake `ZATCA-SIM-*` ID. | `lib/zatca.ts:63-64` (name/VAT), `:117-121` (simulation) | If a real payment webhook ever fires (the payment backend is fully coded — see P0-7), any customer who requests an invoice for expense/reimbursement — again, precisely the corporate segment being targeted — receives an official-looking VAT document for a company that isn't the one they booked with, with a fabricated VAT number. This is a direct legal/compliance liability, not just a data bug. | S | Read `sellerName` from `credentials.legalEntityName` and refuse to build/submit an invoice (return an explicit error, not a fake ID) when `vatNumber`/API credentials are absent, instead of silently faking success. |
| P0-7 | P0 | Conversion funnel | A fully-implemented payment backend exists (real Moyasar/HyperPay API calls, real Stripe/HyperPay/Moyasar webhook handlers writing `Payment`/`Booking` records) but **no UI component anywhere calls `/api/payments/create-session` or `/api/payments/initiate`**. The booking wizard explicitly documents payment as not connected: a code comment states the gateway isn't live and the final step displays "No Payment Required Now" with cash/card-to-driver copy. | `app/book/page.tsx:112-113` (comment), `:1210,1213-1215` (UI copy); orphaned endpoints: `app/api/payments/create-session/route.ts`, `app/api/payments/initiate/route.ts` | No online payment capture, no deposit, no pre-authorization is possible for ANY booking today, despite the backend existing — every booking is a request that waits for manual driver/dispatcher cash handling. For corporate/international/VIP bookers (the stated growth targets) who expect to pay by card up front, this is a hard stop, not friction. | M | Add a "Pay deposit now" step to `app/book/page.tsx` calling the already-built `/api/payments/create-session`; the backend work is done, only the UI wiring is missing. |
| P0-8 | P0 | Conversion funnel | Both B2B lead paths — `/partners` (hotels/agencies/corporate) and the Corporate Account form — POST to `/api/contact`, which has **no database write at all**; it only sends an email via Resend inside a best-effort `Promise.allSettled`. | Forms: `app/partners/page.tsx:151,157,164`, `components/booking/CorporateAccountForm.tsx:29,36`. Endpoint: `app/api/contact/route.ts` (no `prisma`/`supabase` import anywhere in the file) | If the Resend send fails for any reason (rate limit, bad address, transient API error), a corporate/hotel/Umrah-operator lead — the highest-LTV segment the owner explicitly wants to grow — is lost with zero trace, unlike consumer leads which fall back to a `Lead` DB row via `/api/leads`. | S | Add a `prisma.lead.create` (or a dedicated `B2BLead` model) write in `/api/contact` before/alongside the email send, matching the pattern already proven in `/api/leads`. |
| P0-9 | P0 | SEO / UX | The `/routes` page's own city filter dropdown is broken: it derives filter options via `fromCity.split(' ')[0]`, which truncates any multi-word city name to its first token — "Al Khobar" becomes "Al", "Doha, Qatar" becomes "Doha,", "Abu Dhabi, UAE" becomes "Abu". | `app/(marketing)/routes/RoutesClient.tsx:18,23` | This is on the money page for the entire route catalog — a visitor trying to filter to their city sees garbled, unprofessional dropdown options ("Al", "Doha,") and reasonably concludes the site is broken or abandoned, exactly as described in 00-FINDINGS.md. Confirmed still present. | S | Filter/group by a dedicated `city` field (or the full `fromCity`/`toCity` string) instead of `split(' ')[0]`. |
| P0-10 | P0 | Data integrity | Route count is stated as three different numbers depending on which page a visitor lands on: "56+" (site-wide stats config), "50+" (routes hub H1 and meta description, locations page CTA), and the actual count in the route data array is 71. | `lib/config/stats.ts:5-6` (56+), `app/(marketing)/routes/RoutesClient.tsx:60` and `app/(marketing)/routes/page.tsx:8` (50+), `app/(marketing)/locations/[city]/page.tsx:249` (50+), actual: `lib/data/routes.ts` (71 `slug:` entries, verified by count) | A small, checkable fact ("how many routes do you cover") stated three different ways sitewide is a low-cost, high-visibility trust leak — easy for a competitor comparison or a skeptical corporate buyer to notice and use against the brand. | S | Compute the displayed count from `ROUTES_DATA.length` everywhere (stats config, hub H1, meta description, CTAs) instead of retyped literals. |

## P1 — growth-blocking

| ID | Severity | Area | Finding | file:line | Business impact | Fix effort | Fix summary |
|---|---|---|---|---|---|---|---|
| P1-1 | P1 | i18n / SEO | The visible language switcher does not link to `/ar` at all — it's a client-side string-swap (`setLanguage()` → React state + `localStorage`) that changes in-page copy without changing the URL. The real, indexable `/ar/*` pages (6 of them, with correct reciprocal hreflang) are reachable only via the sitemap, never via any on-page link. | `components/shared/LanguageSwitcher.tsx` (no `href="/ar"` anywhere), used in `components/layout/Navbar.tsx:261,366` | Arabic searchers — explicitly called out by the owner as the highest-value in-Kingdom and corporate/VIP segment — who land on the English site have no discoverable path to the Arabic version via the UI; the two language experiences (URL-based `/ar/*` vs in-page string-swap) are disconnected systems that don't reinforce each other. | S | Change the switcher's "Arabic" option to `<Link href="/ar">` (falling back to the closest real `/ar/*` equivalent per `AR_REAL_ROUTES`) instead of a pure client-state toggle. |
| P1-2 | P1 | i18n | Only 6 of ~130+ indexable page templates (routes, locations, services, blog, guides — everything except home/about/contact/faq/pricing/partners) have any Arabic content or hreflang. The root `<html>` tag is hardcoded `lang="en"` even when serving the real `/ar/*` pages. | `lib/config/i18n.ts:2` (6-route allowlist), `app/layout.tsx:223` (`<html lang="en" ...>` with no per-locale branch) | The 71 route pages and 12 location pages — the actual money pages for Umrah/airport-transfer intent — are entirely invisible to Arabic search, and even the 6 pages that do have Arabic content mis-declare their language to crawlers/screen readers via the hardcoded `lang="en"`. | M (lang fix) / L (extending Arabic to route/location templates) | Make `<html lang>` conditional on the resolved locale in `SiteShell`/root layout; prioritize Arabic translation for the highest-value route/location templates before more English-only content templates. |
| P1-3 | P1 | Conversion funnel | Analytics fires only 6 distinct events (`quote_generated`, `lead_captured`, `whatsapp_click`, `price_calculated`, `vehicle_selected`, `booking_started`); two events are declared in the type union but never fired anywhere in the codebase: `booking_completed` and `phone_click`. | Declared-but-dead: `lib/analytics.ts:4-11`; fired events confirmed via grep across `app/` and `components/` | There is no way to measure how many bookings actually complete or how many visitors call instead of messaging — the funnel's two "did we actually make money" signals are silently missing, so marketing spend and funnel-fix priorities are being decided without the one number that matters most. | S | Fire `trackEvent("booking_completed", ...)` in `/api/bookings`'s success path (or client-side on `app/book/page.tsx` step 6) and `trackEvent("phone_click", ...)` on every `tel:` link. |
| P1-4 | P1 | SEO / content | `ServiceRelatedLinks` — rendered on all 18 service pages — uses a fixed, hardcoded set of 8 routes and 6 cities regardless of the calling page's topic; only those 8/71 routes and 6/12 cities receive any inbound link from the entire services section. | `components/seo/ServiceRelatedLinks.tsx:8-42` (static `SERVICES`/`ROUTES`/`CITIES` arrays, not filtered by topic) | Most of the route/location catalog gets no internal-linking support from the services section at all, weakening crawl depth and topical relevance signals for exactly the long-tail, low-competition pages (per 00-FINDINGS' "where the money actually is" list) that most need internal links to rank. | M | Compute related routes/cities per service page (e.g. by shared keyword/tag with the service, similar to the algorithmic approach already built in `RouteRelatedLinks.tsx`) instead of one static list reused everywhere. |
| P1-5 | P1 | Rendering / CWV | `components/sections/home-page.tsx` is a single 1,671-line `"use client"` component containing all three languages of copy, FAQ text, testimonial markup, and static feature grids — only a small slice (a hero slideshow's `useState`/`useEffect` and a few `onClick` scroll handlers) actually requires client JS. | `components/sections/home-page.tsx:1` (`"use client"` for the whole file) | The homepage is the highest-traffic, highest-bounce-risk page and the one most likely to be opened on a mid-range phone on a Saudi mobile network (the stated pilgrim/traveler audience) — shipping ~1,670 lines of largely static markup as client JS directly costs LCP/TBT on exactly that device profile. | L | Split into a server component shell (copy, structure, static sections) with small client "islands" for the slideshow and scroll-triggered buttons only. |
| P1-6 | P1 | Data integrity | `/routes` (the hub/grid page) fetches from a Postgres `Route` table populated by a separate seed script, not directly from `ROUTES_DATA`; falls back to `ROUTES_DATA` only if the DB call errors or returns empty. Recent commits (`358aef8`, `8d3ffc4`) are explicitly patching "live-DB drift" between the two. | `app/(marketing)/routes/page.tsx:29-46` (`db.route.findMany()` with `ROUTES_DATA` fallback) | The route grid a visitor filters/browses can show different facts than the individual route detail pages (which build from `ROUTES_DATA` directly via `generateStaticParams`) and the sitemap (also `ROUTES_DATA`-driven) — this is an active, recurring bug pattern (evidenced by repeated recent hotfix commits), not a one-off. | M | Either make the Prisma `Route` table the single real source (generate route pages from a DB query instead of `ROUTES_DATA`), or delete the DB table and always render `/routes` directly from `ROUTES_DATA` — stop maintaining both. |
| P1-7 | P1 | Conversion funnel | No exit-intent capture and no server-side abandoned-quote recovery exist. `app/book/page.tsx` does persist a draft, but only to `sessionStorage` (client-side, device-local) — never sent to the server, so it cannot power any follow-up. | Confirmed absent by whole-repo grep for `beforeunload`/`exit-intent`; local-only draft: `app/book/page.tsx:161-169` (`persistProgress` → `sessionStorage.setItem`) | Every visitor who starts filling the booking wizard or price calculator and leaves before finishing (the majority, by typical funnel math) is unrecoverable — no name/phone is captured until the very last step, and there is no remarketing signal at all for this segment. | M | POST a partial-lead record to `/api/leads` as soon as pickup+dropoff+date are known (mirroring what `PriceCalculator.tsx` already does before its WhatsApp hand-off), instead of only persisting locally. |
| P1-8 | P1 | Trust | Testimonials (`CITY_DETAILS[city].testimonials`) are fully authored, undated, unsourced quotes with invented names/locations, rendered as plain UI (correctly NOT wrapped in Review/AggregateRating JSON-LD — that specific risk from 00-FINDINGS is not reproduced). | `lib/data/locations.ts:38-42,78-82,153-157` (example entries); rendered at `app/(marketing)/locations/[city]/page.tsx:316-343` | A careful buyer (the corporate/VIP segment specifically) who notices testimonials have no dates, no verification, and generic stock-photo-style names/locations will discount all trust signals on the page, including the real ones (licensing, fixed pricing). | M | Either replace with real, attributable reviews (even a handful, sourced from actual WhatsApp/email feedback with permission) or clearly label the section as illustrative, and add real dates. |
| P1-9 | P1 | Trust | The claimed "1–2 hour" manual confirmation window is stated twice on the homepage directly beside language promising an "Instant Price Calculator" and "Guaranteed Fixed Quote." | `components/sections/home-page.tsx:111,185` | For airport-arrival and Umrah-transfer intent — people booking from an arrivals hall — a 1–2 hour wait after being promised "instant" reads as bait-and-switch and pushes the booking to Careem/Uber/a competitor who confirms in seconds. Directly named in 00-FINDINGS as likely the largest single explanation for missing enquiries. | S (copy) / L (making it actually instant, see P0-2) | Either fix the underlying pricing engine so quotes really are instant and bookable without manual confirmation (see P0-2, P0-7), or remove the "instant"/"guaranteed" language until that's true — don't advertise a promise the funnel doesn't keep. |

## P2 — debt

| ID | Severity | Area | Finding | file:line | Business impact | Fix effort | Fix summary |
|---|---|---|---|---|---|---|---|
| P2-1 | P2 | Backend architecture | Prisma and Supabase both operate against the same Postgres database via two separate client libraries; `leads`/`notification_failures` tables are provisioned by a raw Supabase SQL migration but actually read/written exclusively through Prisma's mapped models. `public.bookings` (Supabase migration `0001`) is orphaned — the live booking flow uses the Prisma `Booking` model instead. `lib/supabase/bookings.ts` is a mislabeled, uncalled dead file that actually queries via Prisma. | `lib/db.ts:4-7` (comment acknowledging the shared pool), `supabase/migrations/0001_bookings_table.sql:4` (orphaned table), `lib/supabase/bookings.ts:1,23` (mislabeled + dead) | Not a live bug, but every new engineer (or AI assistant) touching booking/lead code has to reverse-engineer which of two ORMs is "real" for a given table before making a safe change — this is exactly the kind of ambiguity that causes a P0-class data bug six months from now. | L | Pick one client library as canonical per table (Prisma for everything currently Prisma-backed; Supabase only for `quotations`/`drivers`), delete the orphaned `public.bookings` table and `lib/supabase/bookings.ts`, and document the split once. |
| P2-2 | P2 | Backend architecture | The Prisma `FareRule` model exists in the schema but has zero callers anywhere in `app/` — pricing is computed entirely from static TypeScript data (`lib/pricing/data/vehicles.ts`, `rules.ts`), never from the database. | `prisma/schema.prisma:249-263` (model), confirmed zero call sites via repo-wide grep | Not urgent, but it's a fully-modeled DB table that implies "pricing is admin-editable" when it isn't — anyone (including a future admin UI) reasonably assuming `FareRule` is live would ship a feature against a table nothing reads. | M | Either wire the admin vehicle/pricing screens to actually read/write `FareRule` (making prices genuinely admin-editable without a redeploy), or remove the unused model. |
| P2-3 | P2 | Dependencies | Full payment SDKs (`stripe`) and `twilio` are installed and have real webhook-handling code paths, despite zero UI entry point into the payment flow (P0-7) and unconfirmed usage of Twilio anywhere in an active send path. `@react-pdf/renderer` plus two bundled Arabic font files exist solely for the quotation-invoice PDF route. Three overlapping UI/primitive systems are present at once (`@radix-ui` — 2 packages, `@base-ui/react`, `shadcn`). | `package.json:15-53` | Server-only dependencies don't cost client bundle size, but they expand the audit/security surface (a webhook handler with real signature verification code sitting unused is still a webhook handler someone must keep patched) and signal an unfinished migration between UI kits to any new contributor. | M | Confirm Twilio usage or remove; once P0-7 ships payment UI, keep Stripe/Moyasar/HyperPay only if actually offered at checkout; standardize on one component-primitive system. |
| P2-4 | P2 | SEO | Sitemap `lastModified` is `new Date()` computed once at build/request time and reused for nearly every URL (locations, sub-areas, airports, routes, fleet, guides, recovery, static, `/ar/*`) — only blog posts use a real per-item `publishedAt` date. | `app/sitemap.ts:59,63,71,78,86,92,100,107,113(real),121,128` | Search engines use `lastmod` as a recrawl-priority signal; a sitemap where 95% of URLs claim to have changed "right now" on every deploy is a weak signal that engines are known to partially discount, slowing recrawl of genuinely updated pages. | S | Track a real `updatedAt` per content item (or at minimum per data file, via git or a manual constant) instead of `new Date()` at request time. |
| P2-5 | P2 | Environment / ops | `.env.example` declares only 2 of the ~20+ environment variables the app actually reads (Resend, NextAuth, Google Maps, ZATCA, Moyasar/HyperPay/Stripe, GTM/GA4/Clarity, admin credentials are all undeclared). | `.env.example:1-3` vs actual `process.env.*` usage across `app/api/**`, `lib/**` | Anyone provisioning a new environment (staging, a new developer, disaster recovery) has no authoritative checklist of what's required — several of the P0 findings above (missing Google Maps key, missing ZATCA vars) are symptoms of exactly this gap. | S | Regenerate `.env.example` from a repo-wide grep of `process.env.` references, with placeholder values and a one-line comment per var. |
| P2-6 | P2 | Analytics / privacy | GTM, GA4, and Microsoft Clarity all load unconditionally via `next/script` as soon as their respective env var is present — gated only on "is the ID configured," not on user consent. | `components/analytics/AnalyticsScripts.tsx:22-41` | For a business explicitly expanding toward GCC/EU-adjacent corporate and international travelers, shipping third-party tracking with no consent gate is a compliance gap that gets more expensive to retrofit the more traffic depends on it. | M | Add a minimal consent gate (even a simple accept-once banner) before firing `AnalyticsScripts`, or confirm this is acceptable under current Saudi PDPL guidance and document that decision. |
| P2-7 | P2 | Dead code / confusion | `components/booking/booking-form.tsx` (a separate 5-step booking form posting to `/api/quotations`, with no live price shown before submission) has zero importers anywhere in the codebase — it is not rendered on any page. The live booking UX is exclusively `app/book/page.tsx`'s 6-step wizard. | Confirmed via repo-wide grep for its import path — no matches | Low direct impact since it's unreachable, but it's exactly the kind of orphaned component that gets "fixed" by a future contributor who doesn't realize it's dead, wasting effort, or worse, gets wired back in without the price-before-submit gap being noticed. | S | Delete the file, or if it's meant to replace `/book` eventually, note that intent in a comment and track it as a real in-progress migration. |
| P2-8 | P2 | Backend / API surface | `app/api/seed-routes/route.ts` and `app/api/setup-admin/route.ts` are one-off maintenance endpoints; their auth protection was not directly re-verified in this pass beyond confirming they exist outside the `middleware.ts` admin-route matcher pattern used for `/api/admin/*`. | `app/api/seed-routes/route.ts`, `app/api/setup-admin/route.ts`; matcher scope: `middleware.ts:21-23,88-92` | UNVERIFIED severity — if either endpoint lacks its own internal auth check (middleware only protects `/api/admin/*` and `/api/admin`-prefixed paths, not these two), they could allow unauthenticated route reseeding or admin account creation in production. | S (to verify) | Read both files directly to confirm an internal auth/secret check exists; if not, add one or move them under `/api/admin/`. |

## P3 — polish

| ID | Severity | Area | Finding | file:line | Business impact | Fix effort | Fix summary |
|---|---|---|---|---|---|---|---|
| P3-1 | P3 | Trust copy | Footer copyright reads "© 2026 Taxi Saudi Arabia. All rights reserved. Created with absolute elegance." — reads as unedited template filler, repeated in AR/UR translations ("صُنع بأناقة مطلقة", "بہترین نفاست کے ساتھ تیار کردہ"). | `components/layout/Footer.tsx:14,26,38` | Small but visible on every page footer; a careful visitor (or competitor) screenshots it as evidence of an unfinished/templated site. | S | Replace with the real legal entity name once available (see P0-5), or simply drop the "created with absolute elegance" clause. |
| P3-2 | P3 | Trust copy | Privacy Policy "Last Updated" date is computed as `January 1, {new Date().getFullYear()}` at render time — always shows the current year regardless of when the policy actually last changed. | `app/privacy-policy/page.tsx:12,34` | Minor but technically misleading — a visitor or auditor checking "when was this last updated" gets a fabricated always-current answer. | S | Replace with a real static `lastUpdated` constant, updated only when the policy text actually changes. |
| P3-3 | P3 | SEO | `app/(marketing)/services/airport-transfers/page.tsx` renders two `<h3>` headings before the page's first `<h2>` — a heading-level skip. | `app/(marketing)/services/airport-transfers/page.tsx:118,149` (h3) before `:159` (first h2) | Minor accessibility/SEO nit — screen readers and crawlers infer document structure from heading order; a skip is a weak signal, not a major one. | S | Promote the two `<h3>`s to `<h2>` or insert the missing `<h2>` ahead of them. |
| P3-4 | P3 | Image quality | Fleet page vehicle image alt text duplicates "Saudi Arabia" twice: `` `${vehicle.name} — Taxi Saudi Arabia Saudi Arabia` ``. | `app/fleet/page.tsx:147` | Cosmetic; slightly degrades alt-text quality for accessibility/image search. | S | Fix the template string. |
| P3-5 | P3 | CWV | Several hero/content `<Image fill>` usages omit the `sizes` prop (defaults to loading the largest source at every breakpoint), inconsistent with other `<Image>` usages in the same codebase that do set it correctly. | `app/(marketing)/routes/[slug]/page.tsx:1203,1268`, `app/(marketing)/locations/[city]/page.tsx:176-183`, `app/(marketing)/services/airport-transfers/page.tsx:84-89`, `components/sections/home-page.tsx:1354-1359` | Minor, page-specific LCP/bandwidth cost on route/location/service hero images — the exact templates used across 71+12+19 pages, so the aggregate cost across the catalog is larger than any single instance. | S | Add a `sizes` attribute matching the ones already correctly set elsewhere in the codebase (e.g. `home-page.tsx:679`). |
| P3-6 | P3 | UX | The floating WhatsApp button shows a hardcoded fake "1" unread-message badge regardless of actual state. | `components/shared/WhatsAppButton.tsx:110-113` | Minor dark pattern; unlikely to meaningfully hurt or help conversion, but is a specific, checkable claim (an "unread message") that isn't real. | S | Remove the badge or tie it to a real signal (e.g. only show after the 4-second prompt is dismissed unread). |
| P3-7 | P3 | Content ops | `lib/data/locations.ts` (`CITY_DETAILS`) is duplicated by a separate hardcoded `LOCATIONS` array in `app/(marketing)/locations/page.tsx` for the hub page's link list — currently in sync (12/12) but not derived from the same source. | `app/(marketing)/locations/page.tsx` (hardcoded array) vs `lib/data/locations.ts` (`CITY_DETAILS`) | Currently harmless; becomes a silent orphan-page bug the next time a city is added to one list and not the other. | S | Derive the hub page's link list from `Object.keys(CITY_DETAILS)` instead of a separate literal array. |
| P3-8 | P3 | Dependencies | `lucide-react` is pinned at `^1.16.0`, an unusual version number for a library whose recent releases use 0.x versioning conventions across most of this era's Next.js ecosystems — worth a manual registry check to confirm the pin is intentional and the imports used actually exist at that version. | `package.json:33` | UNVERIFIED — flagged for a 2-minute manual check (`npm view lucide-react versions`), not confirmed broken. | S | Verify the pin against the npm registry; correct if it's a typo'd major version. |

---

**UNVERIFIED items requiring production/external access** (cannot be confirmed from the codebase alone — listed here rather than guessed):

1. **Production Google Maps key status** (P0-2) — verify with `vercel env ls` against the live Vercel project, or inspect a live network request to `/api/pricing` in production dev tools for a `distancematrix` call vs an immediate fabricated response.
2. **Production ZATCA/Moyasar/HyperPay/Stripe env vars** (P0-6, P0-7) — same `vercel env ls` check.
3. **Whether `/api/seed-routes` and `/api/setup-admin` are reachable unauthenticated in production** (P2-8) — requires either reading both files' internal auth checks (quick, recommend doing this before deploying any fix) or an authenticated-vs-anonymous curl test against the live site.
4. **Google Business Profile existence/review count** — outside the codebase entirely; check https://business.google.com or a Maps search for "Taxi Saudi Arabia" directly.
