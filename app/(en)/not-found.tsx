// English in-app 404 (rendered inside the (en) root layout when a page calls
// notFound()). The global/unmatched-route 404 lives at app/not-found.tsx.
export { default } from "@/components/layout/NotFoundContent";
